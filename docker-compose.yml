services:
  bannatiperunpisellobot:
    build:
      context: .
      dockerfile: Docker/Dockerfile
    volumes:
       - ./logs:/usr/src/app/logs
    env_file:
       - .env
    restart: unless-stopped
    network_mode: "host"
